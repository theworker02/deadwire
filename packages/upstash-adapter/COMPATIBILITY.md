# Upstash compatibility contract

Treat Upstash Workflow/QStash as execution authority. Use documented DLQ and resume interfaces only. Unknown provider fields must not be promoted to trusted evidence. Retain raw request/response bodies only when a customer explicitly configures a redacted evidence vault.
