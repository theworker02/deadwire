# Agent metrics

Track model input/output tokens, estimated cost, tool-call count, state fingerprints, semantic progress, termination reason, and checkpoint position. Alert only on actionable loop/budget conditions to avoid noisy agent telemetry.
