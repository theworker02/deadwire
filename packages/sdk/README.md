# `@deadwire/sdk`

The SDK records only the application-owned boundary around durable steps and tools. It emits source hashes, ordered step manifests, deployment provenance, hashed idempotency/provider references, and shape fingerprints through a signed emitter.
