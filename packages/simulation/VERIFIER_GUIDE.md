# Verifier guide

Implement verifiers against a payment ledger, delivery ledger, transactional outbox, object inspector, or sandboxed HTTP contract endpoint. Return `absent`, `confirmed`, `idempotent`, or `unknown` with human-readable evidence and a check timestamp.
