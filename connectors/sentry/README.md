# Sentry connector

The Sentry connector is an optional evidence enrichener. It should link grouped errors, release identifiers, and sanitized stack fingerprints to Deadwire incidents without duplicating sensitive event payloads.
