# Sentry privacy boundary

Import only error ID, fingerprint, release, environment, timestamp, and allowlisted tags by default. Retrieve full event content only through an explicit operator request and short-lived encrypted access path.
