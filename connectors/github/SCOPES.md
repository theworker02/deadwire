# Required GitHub scopes

Use a fine-grained token limited to the relevant repository with read-only Contents and Metadata permissions. Do not use an organization administrator token. Store the token only in the trusted connector environment.
