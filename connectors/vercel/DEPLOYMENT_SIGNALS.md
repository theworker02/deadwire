# Vercel deployment signals

Capture deployment ID, target environment, commit SHA/ref, created timestamp, and project identity. Enrich with GitHub comparison data when possible; a Vercel event alone does not prove code compatibility.
